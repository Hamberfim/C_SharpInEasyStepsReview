<!-- remove decorator'at'model _01_JokesWebApp.Models.Joke - not creating a new joke- using it as a temple to creat a data entry form-->
<h4>Search Jokes</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ShowSearchResults">
            <!-- Above ^^^ replaced <form asp-action="ShowSearchForm"> with <form asp-action="ShowSearchResults"> because the action is not to show the form agian but some results -->
            <!-- <div asp-validation-summary="ModelOnly" class="text-danger"></div> validation not needed -->
            <!-- <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
                <input asp-for="Id" class="form-control" />
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="JokeQuestion" class="control-label"></label>
                <input asp-for="JokeQuestion" class="form-control" />
                <span asp-validation-for="JokeQuestion" class="text-danger"></span>
            </div> -->
            <div class="form-group">
                <label name="SearchPhrase" class="control-label"></label>  <!-- replaced asp-for="JokeAnswer" with name="SearchPhrase" -->
                <input name="SearchPhrase" class="form-control" />  <!-- replaced asp-for="JokeAnswer" with name="SearchPhrase" -->
                <!-- <span asp-validation-for="JokeAnswer" class="text-danger"></span> validation not needed -->
            </div>
            <div class="form-group">
                <input type="submit" value="Search" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

<!-- validation not need since this is not creating a new record
remove decorator'at'section Scripts {
    remove decorator'at'{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} -->
